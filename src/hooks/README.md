# hooks/

Custom React hooks for the application.

Place reusable stateful logic here (e.g. `useWardrobe.ts`, `useOutfitGenerator.ts`)
to keep page and component files lean and focused on rendering.

Hooks in this folder should:
- Start with the `use` prefix
- Not contain JSX
- Have a clear single responsibility
